# task1
 Password Strength Checker + Hash Generator (CLI Based) Objective


Short explanation of how hashing works :
It converts any text into a string of letters and numbers using a mathematical algorithm. It's one-way and cannot return from the hash to the original password; if an attack occurs, the original password cannot be seen
